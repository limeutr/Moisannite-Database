<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css2?family=Indie+Flower&display=swap" rel="stylesheet">
    <title>Moissanite</title>
    <style>
        body {
            font-family: 'Arial', sans-serif; 
            background-color: #fce4ec; 
            padding: 0;
        }
        .container {
            max-width: 900px;
            margin: 0 auto;
            padding: 20px;
        }
        
        .pink-bg {
            background-color: #f48fb1; 
            padding: 20px;
            border-radius: 8px;
        }
       
        .row {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
        }
        .col-md-4 {
            margin-bottom: 20px;
        }
        .card {
            background: linear-gradient(135deg, #ff69b4 0%, #ffb6c1 100%); /* Dual pink gradient */
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
            margin-bottom: 20px;
            padding: 15px;
            overflow: hidden;
            color: white; /* Text color for contrast */
        }
        .card img {
            width: 100%;
            height: auto;
            max-height: 300px; 
            object-fit: scale-down; 
        }
        .card-img-top {
            border-top-left-radius: 8px;
            border-top-right-radius: 8px;
            object-fit: cover;
            height: 200px;
        }
        .card-body {
            padding: 20px;
        }
        .card-title {
            font-size: 18px;
            font-weight: bold;
            color: #ec407a; 
            margin-bottom: 10px;
        }
        .btn-primary,
        .btn-secondary,
        .btn-warning,
        .btn-danger {
            background-color: #ec407a; 
            color: white;
            margin-right: 5px;
            margin-bottom: 5px;
            padding: 8px 12px;
            border-radius: 4px;
            font-size: 14px;
            cursor: pointer;
        }
        .btn-primary:hover,
        .btn-secondary:hover,
        .btn-warning:hover,
        .btn-danger:hover {
            background-color: #d81b60; 
        }
        .add-button {
            position: absolute;
            top: 10px;
            right: 40px;
            background-color: #000000cf;
            size-adjust: auto;
        }
    </style>
</head>
<body>
    <nav style="background-color: #e86b95; padding: 10px; text-align: center;">
        <a href="/" style="color: white; font-family: 'Indie Flower', cursive; font-size: 24px;">.˚｡୨୧⋆Moissanite⋆୨୧˚.</a>
    </nav>
    <div class="container">
        <br>
        <a href="/add" class="btn btn-primary add-button">Add Image</a>
        <div class="row">
            <% if (images.length > 0) { %>
                <% images.forEach(image => { %>
                    <div class="col-md-4">
                        <div class="card">
                            <img src="/images/<%= image.filename %>" class="card-img-top" alt="<%= image.description %>">
                            <div class="card-body">
                                <h5 class="card-title"><%= image.description %></h5>
                                <a href="/image/<%= image.id %>" class="btn btn-primary">View</a>
                                <a href="/download/<%= image.filename %>" class="btn btn-secondary">Download</a>
                                <a href="/updateImage/<%= image.id %>" class="btn btn-warning">Update</a>
                                <a href="/delete/<%= image.id %>" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this image?')">Delete</a>
                            </div>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <div class="text-center"><h3>No images found in the database</h3></div>
            <% } %>
        </div>
    </div>
</body>
</html>
