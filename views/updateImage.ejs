<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Image</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #ffd1dc;
            color: #333;
            margin: 0;
            padding: 0;
        }

        .container {
            width: 80%;
            margin: auto;
            padding-top: 20px;
        }

        .header {
            text-align: center;
            font-family: 'Comic Sans MS', cursive, sans-serif;
            color: #ff69b4;
            margin-bottom: 20px;
        }

        .form-container {
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
        }

        .form-container label {
            font-weight: bold;
        }

        .form-container input[type="text"],
        .form-container input[type="file"] {
            width: calc(100% - 20px);
            padding: 10px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
        }

        .form-container input[type="submit"] {
            background-color: #ff69b4;
            color: white;
            border: none;
            padding: 10px 20px;
            cursor: pointer;
            border-radius: 4px;
        }

        .form-container input[type="submit"]:hover {
            background-color: #d4418e;
        }
        .btn {
            display: inline-block;
            background-color: #e95fa4;
            color: white;
            padding: 10px 15px;
            text-decoration: none;
            border-radius: 4px;
            font-size: 12px;
            font-weight: bold;
            margin-top: 10px;
        }
        .btn:hover {
            background-color: #d81b60;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            <h2>Updating Image...</h2>
        </div>
        <div class="form-container">
            <% if (moisannite) { %>
                <form action="/updateImage/<%= moisannite.id %>" method="POST" enctype="multipart/form-data">
                    <label for="image">~Current Image~</label><br>
                    <img src="/download/<%= moisannite.filename %>" alt="Current Image"><br><br>

                    <input type="hidden" id="currentImage" name="currentImage" value="<%= moisannite.filename %>">
                    <img src="<%= moisannite.image %>" width="20%"><br><br>
                    <label for="description">~Description~</label><br>
                    <input type="text" id="description" name="description" 
                    value="<%= moisannite.description %>"><br><br>~New Image~<br>
                    <input type="file" id="image" name="image" accept="image/*"><br><br>
                    <input type="hidden" id="id" name="id" value="<%= moisannite.id %>"><br><br>
                    <input type="submit" value="Update Image">
                </form>
                <% } else { %>
                    <p>No image found >_0</p>
                    <% } %>
                        <a href="/" class="btn">Back to Home?</a>
        </div>
    </div>
</body>

</html>